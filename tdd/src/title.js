const React = require('react');

/**
 * - Fazer os Testes unitários no lado do servidor (node), será mais rápido
 */

const Title = () => (
  React.createElement('h1', {}, 'Title')
)

module.exports = Title